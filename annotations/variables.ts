let apples = 5;
let speed: string = "fast";

//arrays

let myNumbers: number[] = [1, 2];

let truths: boolean[] = [true, false];

///class
class Car {}

let car: Car = new Car();

//object liteals

let point: { x: number; y: string } = {
  x: 10,
  y: "2"
};

//function

const asdf = 1;

const logNumber: (i: number) => void = asdf => {
  console.log(asdf);
};

logNumber(asdf);
